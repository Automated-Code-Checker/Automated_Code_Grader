#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
    int sum=0,sub=0;
    
    scanf("%d %d", &a, &b);
    sum=a+b;
    sub=b-a;
    printf("%d\n%d", sum, sub);

    return 0;
}
