#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b,c,d,e;
    
    scanf("%d %d", &a, &b);
    c=a+b;
    d=a-b;
    e=-1*d;
    printf("%d\n%d", c, e);

    return 0;
}
