#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
    //int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    //update(pa, pb);
    //printf("%d\n%d", a, b);
    printf("%d\n",a+b);
    int e=a-b;
    if(e==e)
    printf("%d",e*(-1));
    return 0;
}
