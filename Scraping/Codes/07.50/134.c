#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b,res,re;
    //int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    res=a+b;
    re=b-a;
    printf("%d\n%d",res, re);

    return 0;
}
