#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
   
   
    scanf("%d %d", &a, &b);
   
while((a-b)>0)
{
     printf("%d\n%d", a+b,a-b);
     break;
}

 printf("%d\n%d", a+b, (a-b)*(-1));
 

    return 0;
}
