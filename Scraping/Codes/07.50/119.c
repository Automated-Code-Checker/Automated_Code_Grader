#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;    
    scanf("%d %d", &a, &b);
    int x=a+b;
    int y=b-a;
    printf("%d\n%d", x, y);

    return 0;
}
