#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
  scanf("%d %d",&a,&b);
  int sum=a+b;
  int diff=b-a;
  printf("%d",sum);
  printf("\n%d",diff);

    
}
