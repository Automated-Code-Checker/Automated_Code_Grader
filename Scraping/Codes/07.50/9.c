#include <stdio.h>

void update(int *a,int *b) {
    int off;
    a<b;
    return off;
    // Complete this function    
}

int main() {
    int a, b,sum1,sum2;
    int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    update(pa, pb);
    sum1=a+b;\
    sum2=b-a;
    printf("%d\n%d", sum1, sum2);

    return 0;
}
