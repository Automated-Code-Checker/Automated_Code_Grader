#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a=4, b=5;
    
    
    scanf("%d %d", &a, &b);
    printf("%d",(a+b));
    printf("\n%d",-(a-b));

    return 0;
}
