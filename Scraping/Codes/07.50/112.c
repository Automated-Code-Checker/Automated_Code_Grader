#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int sum,diff;
    int a;
    int b;
    scanf("%d %d", &a, &b);
    scanf("%d%d",&sum,&diff);
    sum=a+b;
    diff=b-a;
    printf("%d\n%d",sum,diff);
    
    return 0;
}
