#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a=4,b=5,c;
    scanf("%d%d", &a,&b);
    if(b+a)
    {
        c=b+a;
        printf("%d",c);
    }
    if(b-a)
    {
        c=b-a;
        printf("\n%d",c);
    }
    return 0;
}
