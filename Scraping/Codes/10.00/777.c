#include <stdio.h>
#include <stdlib.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a,c,d,b;
    scanf("%d",&a);
    scanf("%d",&b);
    printf("%d",a+b);
    c=a-b;
    printf("\n%d",abs(c));
    
    return 0;
}