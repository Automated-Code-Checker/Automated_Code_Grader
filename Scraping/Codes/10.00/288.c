#include <stdio.h>
#include<math.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b,n1,n2;
    scanf("%d",&n1);
    scanf("%d",&n2);
    a=n1+n2;
    b=abs(n1-n2);
    
    printf("%d\n%d\n",a,b);

    return 0;
}